using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ClassManager.Common
{
    public enum ErrorCodes
    {
        FATAL = 0,
        SUCCESS = 1,
        DUPLICATE_RECORD = 3,
        RECORD_IN_USER = 4,
        DUPLICATE_ROLLNO = 51,
        RECORD_NOT_EXIST = 52,
        ADMISSION_NO = 53,
        DUPLICATE_MEMSHIP_N = 54
    }
}
